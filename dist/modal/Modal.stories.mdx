import { Canvas, Meta, Story, ArgsTable, Description, DescriptionType, ColorPalette, StoryTable } from '@storybook/addon-docs';
import { Modal } from "./Modal";

<Meta title="MDX/Modal" component={Modal} argTypes={{ title: { description: "desc123" } }} />

export const Template = (args) => <Modal {...args} />

# Modal Component

<Description type={DescriptionType.INFO}>
    This is modal component. Please, use it for all confirmation actions
</Description>

<Canvas>
    <Story
        name="Modal"
        args={{
            visible: false,
            setVisible: () => {},
            buttonsGroup: {
                primary: {
                    label: "Accept",
                    onClick: () => {}
                },
                secondary: {
                    label: "Cancel",
                    onClick: () => {}
                }
            },
            content: "TEST CONTENT"
        }}
        argTypes={{ visible: { control: "boolean", description: "Set Visible True", name: "visible" } }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={Modal} />